### Information theory and thermodynamics

Version 7.1.4 resolves the numerical collapses previously associated with structural entropy calculations. This stability permits the dispassionate analysis of how information is stored, transmitted, and dissipated across curved manifolds.

### Information geometry and manifold capacity

The Poincaré disk is a fundamental model in information geometry, where distances represent the distinguishability between probability distributions.

- **Metric-weighted entropy scaling**: the telemetry pipeline now weights entropy by the volume element $\sqrt{|g|} = 4 / (1 - r^2)^2$. This allows researchers to quantify the "information capacity" of the manifold as it approaches the boundary. Because the volume element is capped at 50.0, the instrument provides a regularised view of hierarchical data structures, where the "density of states" increases exponentially towards the perimeter.
    
- **Isotropic distinguishability**: the resolution of metric distortion ensures that information gradients are uniform. This is a prerequisite for studying the "Fisher information metric" within a simulated environment, as distinguishability between states must remain independent of the orientation of the observer.
    

### Non-equilibrium thermodynamics and dissipation

The inclusion of the `params.cooling` term and the universal renormalisation group (RG) pass enables the study of dissipative systems and phase space trajectories.

- **Decoherence and friction**: the $(1 - \text{params.cooling})$ multiplier acts as a thermodynamic sink. Researchers can observe the "Arrow of Time" by injecting energy via the manual brush and tracking how the structural entropy decays back toward macroscopic order. This facilitates the study of non-equilibrium steady states (NESS) where energy injection is balanced by cooling.
    
- **Phase space stability via RG**: the `safe_len` normalisation ensures that the system stays on a physical manifold ($|q| < 1.0$). In thermodynamic terms, this represents the preservation of probability; the total intensity of the field cannot exceed the limits defined by the unit disk, preventing the "unphysical" divergence of the partition function.
    

### Topological data analysis (TDA) and spectral entropy

The real-time Fast Fourier Transform (FFT) and defect telemetry provide a dual-domain view of system order.

- **Spectral power density (PSD)**: the transition to a 2D radial PSD allows for the identification of "thermal noise" versus "coherent signals". Information theorists can use this to measure the signal-to-noise ratio (SNR) of wave propagation through the bulk, identifying resonant frequencies that may correspond to stable information-bearing modes.
    
- **Topological entropy**: the "vortices" telemetry tracks the density of topological defects. In many systems, the transition to turbulence is marked by the proliferation of these defects. Version 7.1.4 allows for the quantitative tracking of how defect density correlates with global structural entropy, providing a numerical test for the Berezinskii–Kosterlitz–Thouless (BKT) transition.
    

### Verifiable computational thermodynamics

Software engineering standardisation in version 7.1.4 ensures that thermodynamic results are reproducible across different hardware architectures.

- **Regularised asymptotes**: by capping the volume element, the instrument avoids the "edge-case" errors that typically plague numerical simulations of hyperbolic space. This ensures that the global averages for energy and entropy are statistically significant and not dominated by sub-pixel fluctuations at the boundary.
    
- **Hardware-independent entropy**: the resolution of the aspect ratio ensures that the "thermodynamic volume" of the simulation is identical on any display. This hardware-independence is critical for benchmarking the efficiency of different information-processing algorithms within the manifold.